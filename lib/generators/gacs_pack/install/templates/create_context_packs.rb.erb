class CreateContextPacks < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :context_packs, id: false do |t|
      t.string :id, null: false, primary_key: true
      t.bigint :tenant_id
      t.jsonb :payload, null: false, default: {}
      t.jsonb :meta, null: false, default: {}

      t.timestamps
    end

    add_index :context_packs, [:tenant_id, :created_at]
    add_index :context_packs, :created_at
  end
end
